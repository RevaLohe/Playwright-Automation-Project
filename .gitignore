# Environment and secrets (never commit)
.env
.env.local
.env.*.local

# Editor / IDE (local only)
.vscode/

# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
tests/dashboard

# Documentation (local only)
docs/
